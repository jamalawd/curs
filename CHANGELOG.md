# Changelog

## 1.0.0

- Launch isolated Cursor logins with `curs <profile>`
- Keep `~/.cursor` and `~/.agents` shared across profiles
- Interactive picker, list, add, edit, remove, which
- Copy primary editor settings on first profile launch
- bash and zsh completions
